
---
title: 'Remote sensing: groen in private buitenruimte'
output:
  html_document:
    df_print: paged
---


```{r, echo=FALSE}
# install & load packages
shhh <- suppressPackageStartupMessages
# packages_needed<-c( 'dplyr', 'here')
# installed<-installed.packages()
# sapply(packages_needed, function(p)
#   if(!p %in% installed[,1]){
#   install.packages(p)
#   }) 
# lapply(packages_needed, library, character.only=TRUE)
# rm(installed, packages_needed)

yr<-2020
neighbourhood <- "BU15810004"
report_tuinen<-FALSE
proj_env <- FALSE

# set directories
#path <- paste0(here::here(),"/REPORT/", neighbourhood)

source('SRC/packages.R')

  #(re)set globals
  source(here::here('SRC/globals.R'))

options(warn=-1)
```


In dit rapport worden de resultaten van de analyse -op hoofdlijnen- getoond. De volgende onderdelen komen aan de orde: 


## rs_rgb:
```{r pl5, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_rgb_buurt_", neighbourhood, ".png"), full.names = TRUE)

knitr::include_graphics(graph.find)
```


## rs_ndvi_vegetation_contours:
```{r pl7, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_ndvi_vegetation_contours_", neighbourhood, ".png"), full.names = TRUE)

knitr::include_graphics(graph.find)
```


## rs_ahn_buurt:
```{r pl8, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_ahn_buurt_", neighbourhood, ".png"), full.names = TRUE)

knitr::include_graphics(graph.find)
```


## rs_rgb_ndvi:
```{r pl18, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_ndvi_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## rs_rgb_vegetation:
```{r pl19, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_vegetation_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## rs_rgb_veg_substantial:
```{r pl20, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_vegetation_substantial_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## rs_rgb_veg_classes_unsupervised:
```{r pl21, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_vegetation_class_unsupervised_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## rs_rgb_veg_classes_fixed:
```{r pl22, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_vegetation_classes_fixed_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## rs_rgb_veg_3m:
```{r pl23, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_vegetation_3m_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## rs_rgb_veg_5m:
```{r pl24, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_vegetation_5m_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## NDVI_mean_garden:
```{r pl25, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_ndvi_mean_tuinen_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```


## rs_gardens_distribution_ndvi_freq:
```{r pl27, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_tuinen_distribution_ndvi_freq_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## rs_gardens_distribution_ndvi:
```{r pl28, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_tuinen_distribution_ndvi_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## green_coverage_garden:
```{r pl29, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_green_cover_tuinen_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## rs_gardens_distribution_vegetation_coverage:
```{r pl30, echo=FALSE, fig.cap=" ", out.width = '100%'}
graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_tuinen_distribution_vegetation_coverage_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## rs_stone_cover_tuinen:
```{r pl31, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_stone_cover_tuinen_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```

## rs_water_cover_tuinen:
```{r pl310, echo=FALSE, fig.cap=" ", out.width = '100%'}

graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_water_cover_tuinen_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```



## rs_green_cover_woningen:
```{r pl32, echo=FALSE, fig.cap=" ", out.width = '100%'}
graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_green_cover_woningen_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```


## rs_woningen_distribution_vegetation_coverage:
```{r pl33, echo=FALSE, fig.cap=" ", out.width = '100%'}
graph.find <- list.files(path = plots.loc, 
                         pattern = paste0("rs_woningen_distribution_vegetation_coverage_", neighbourhood, ".png"), 
                         full.names = TRUE)

knitr::include_graphics(graph.find)
```
